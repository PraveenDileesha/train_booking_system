// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: trip_fares.sql

package generated

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createTripFare = `-- name: CreateTripFare :one
INSERT INTO trip_fares (trip_id, class, is_reservable, rate_per_km)
VALUES ($1, $2, $3, $4)
RETURNING id, trip_id, class, is_reservable, rate_per_km
`

type CreateTripFareParams struct {
	TripID       int32          `json:"trip_id"`
	Class        CoachClass     `json:"class"`
	IsReservable bool           `json:"is_reservable"`
	RatePerKm    pgtype.Numeric `json:"rate_per_km"`
}

func (q *Queries) CreateTripFare(ctx context.Context, arg CreateTripFareParams) (TripFare, error) {
	row := q.db.QueryRow(ctx, createTripFare,
		arg.TripID,
		arg.Class,
		arg.IsReservable,
		arg.RatePerKm,
	)
	var i TripFare
	err := row.Scan(
		&i.ID,
		&i.TripID,
		&i.Class,
		&i.IsReservable,
		&i.RatePerKm,
	)
	return i, err
}

const getTripFare = `-- name: GetTripFare :one
SELECT id, trip_id, class, is_reservable, rate_per_km
FROM trip_fares
WHERE trip_id = $1 AND class = $2 AND is_reservable = $3
`

type GetTripFareParams struct {
	TripID       int32      `json:"trip_id"`
	Class        CoachClass `json:"class"`
	IsReservable bool       `json:"is_reservable"`
}

func (q *Queries) GetTripFare(ctx context.Context, arg GetTripFareParams) (TripFare, error) {
	row := q.db.QueryRow(ctx, getTripFare, arg.TripID, arg.Class, arg.IsReservable)
	var i TripFare
	err := row.Scan(
		&i.ID,
		&i.TripID,
		&i.Class,
		&i.IsReservable,
		&i.RatePerKm,
	)
	return i, err
}

const listTripFares = `-- name: ListTripFares :many
SELECT id, trip_id, class, is_reservable, rate_per_km
FROM trip_fares
WHERE trip_id = $1
ORDER BY class, is_reservable
`

func (q *Queries) ListTripFares(ctx context.Context, tripID int32) ([]TripFare, error) {
	rows, err := q.db.Query(ctx, listTripFares, tripID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []TripFare
	for rows.Next() {
		var i TripFare
		if err := rows.Scan(
			&i.ID,
			&i.TripID,
			&i.Class,
			&i.IsReservable,
			&i.RatePerKm,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
